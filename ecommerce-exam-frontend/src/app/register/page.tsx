import Authentication from '@/components/authentication/Authentication'
import React from 'react'

const Register = () => {
  return (
      <div><Authentication isLoginComponent={false} /></div>
  )
}

export default Register
